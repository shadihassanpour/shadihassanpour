from nornir import InitNornir
from nornir_napalm.plugins.tasks import napalm_get
from nornir_utils.plugins.functions import print_result

nr = InitNornir(config_file="config.yaml")

def napalm_get_example(task):
    task.run(task=napalm_get, getters=[ "get_interfaces", "get_interfaces_ip", "get_config"])

results=nr.run(task=napalm_get_example)
print_result(results)







#def napalm_get(task: Task,getters: List[str]) ,task name is napalm_get , so import it from nornir_napalm.plugins.tasks
#feature of napalm : is independed vendor , at napalm we have some getter ( get_interfaces,get_interfaces_ip,....) but supported some vendor 
#napalm is ,structure output.
#disadvantages  of napalm: just for monitoring command like show ,doese not include all of the command 
#Supported Devices :https://napalm.readthedocs.io/en/latest/support/index.html 
#u can used napalm_cli.py for cli-command like scrapli,netmiko,.... and dont user getters
#def napalm_cli(task: Task, commands: List[str]) task name is napalm_cli, so import from nornir_napalm.plugins.tasks.notice following example:


from nornir import InitNornir
from nornir_napalm.plugins.tasks import napalm_cli
from nornir_utils.plugins.functions import print_result

nr = InitNornir(config_file="config.yaml")

def napalm_cli_commands_example(task):
    task.run(task=napalm_cli, commands=["show  interface desc ", "show version "])

results=nr.run(task=napalm_cli_commands_example)
print_result(results)
